// Input Data Messages (IDM): most chains allow for data to be added to messages (like ordinals). This page allows you to view the data associated with a message.

// TODO: implement paramaterized IDM page, takes address from params and displays IDM data for that address

function IDM() {
  return (
    <div className="page flex align-center">
      <h1>
        Input Data Messages (IDM): most chains allow for data to be added to
        messages (like ordinals). This page allows you to view the data
        associated with a message.
      </h1>
    </div>
  );
}

export default IDM;
